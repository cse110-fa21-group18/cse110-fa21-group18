/*
describe('Basic user flow for Website', () => {
    // First, visit the lab 8 website
    beforeAll(async () => {
      await page.goto('http://127.0.0.1:5501/source/Spatch/public/home.html');
    });
});

 //Check that all filters work
 it('Search Page Results - Check for all results', async () => {
    console.log('Checking for all recipe rsult cards...');

    // expect(accurateResults).toBe(true);
  });


 //Testing to see if all recipe card results showed up for a valid entry
 it('Search Page Results - Check for all results', async () => {
    console.log('Checking for all recipe rsult cards...');

    // expect(expectedResultCards).toBe(tru);
  });
  */



   